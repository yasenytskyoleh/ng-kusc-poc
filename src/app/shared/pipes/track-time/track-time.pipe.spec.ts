import { TrackTimePipe } from './track-time.pipe';

describe('TrackTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TrackTimePipe();
    expect(pipe).toBeTruthy();
  });
});
